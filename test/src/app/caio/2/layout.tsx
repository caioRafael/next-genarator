
import { ReactNode } from 'React'
export default function 2Layout({children}: {children: ReactNode}){
    return(
        <div>
            <h1>2 layout</h1>
            {children}
        </div>
    )
}
